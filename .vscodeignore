# Sources and build config (the compiled output in out/ is what ships)
src/**
tsconfig.json
eslint.config.mjs

# Tooling and dev files
.github/**
.vscode/**
.gitignore
.vscodeignore
package-lock.json

# Source maps and TS declaration files
out/**/*.map
out/**/*.d.ts

# README screenshot (Marketplace renders it from the GitHub raw URL, no need to ship it)
images/glvs-sample.png

# Misc
**/.DS_Store
